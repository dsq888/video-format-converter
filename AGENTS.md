# 项目开发准则（Karpathy Guidelines）

本项目所有编码工作遵循以下 4 条准则，源自 Andrej Karpathy 对 AI 编码常见错误的观察。

## 1. 先想再写

- 不要假设、不要隐藏困惑，主动说明假设与取舍。
- 不确定就问；有多种理解就把它们摆出来，而不是默默挑一个。
- 有更简单的做法就说出来，该反驳时就反驳。

## 2. 保持简单

- 只写解决问题的最少代码，不做投机性设计。
- 不为一次性代码建抽象；不添加没被要求的"灵活性/可配置"。
- 不为不可能发生的场景加错误处理。

## 3. 外科手术式改动

- 只改该改的；不顺手"优化"无关的代码、注释、格式。
- 不重构没坏的东西；遵循现有代码风格。
- 自己改动造成的孤儿引用要清理；但不删既有的死代码（除非被明确要求）。

## 4. 目标驱动

- 把任务转成可验证的目标，用测试让它们通过。
- 多步任务先列短计划，每步标注验证方式。